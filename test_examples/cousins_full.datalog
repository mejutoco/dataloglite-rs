/* Base facts: Parent-Child relationships */
parent("Alice", "Bob").
parent("Alice", "Barbara").
parent("Bob", "Charlie").
parent("Bob", "Cindy").
parent("Barbara", "David").
parent("Barbara", "Diana").
parent("Charlie", "Eve").
parent("Cindy", "Frank").
parent("David", "Grace").
parent("Diana", "Henry").

/* Rules for basic relationships */

// X is a father of Y if X is male and parent of Y
father(X, Y) :- parent(X, Y), male(X).

// X is a mother of Y if X is female and parent of Y
mother(X, Y) :- parent(X, Y), female(X).

// X and Y are siblings if they share at least one parent
sibling(X, Y) :- parent(Z, X), parent(Z, Y), X != Y.

// X is a brother of Y if they are siblings and X is male
brother(X, Y) :- sibling(X, Y), male(X).

// X is a sister of Y if they are siblings and X is female
sister(X, Y) :- sibling(X, Y), female(X).

/* Extended family relationships */

// X and Y are cousins if their parents are siblings
cousin(X, Y) :- parent(A, X), parent(B, Y), sibling(A, B).

// X is an uncle of Y if X is brother of Y's parent
uncle(X, Y) :- parent(Z, Y), brother(X, Z).

// X is an aunt of Y if X is sister of Y's parent
aunt(X, Y) :- parent(Z, Y), sister(X, Z).

// X is a grandparent of Y if X is parent of Y's parent
grandparent(X, Y) :- parent(X, Z), parent(Z, Y).

// X is a grandfather of Y if X is father of Y's parent
grandfather(X, Y) :- father(X, Z), parent(Z, Y).

// X is a grandmother of Y if X is mother of Y's parent
grandmother(X, Y) :- mother(X, Z), parent(Z, Y).

/* Gender facts (for our example family) */
male("Bob").
male("Charlie").
male("David").
male("Frank").
male("Henry").
female("Alice").
female("Barbara").
female("Cindy").
female("Diana").
female("Eve").
female("Grace").

/* Queries can be made against this database */
// For example: 
// ?- cousin(X, Y).
// ?- uncle("Bob", X).
// ?- grandmother(X, "Grace").